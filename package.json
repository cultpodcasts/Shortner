{
  "name": "shortner",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^24.9.2",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.0.4",
    "wrangler": "^4.45.2"
  },
  "dependencies": {
    "@cloudflare/vitest-pool-workers": "^0.10.2",
    "uuid": "^10.0.0",
    "vitest": "^3.2"
  }
}
